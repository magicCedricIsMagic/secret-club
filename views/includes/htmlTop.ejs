<!DOCTYPE html>
<html
	lang="fr"
	class="<%= locals.user ? "dark-theme" : "" %>"
	<% if (locals.user) { %>
		style="
			--user-col: oklch(from <%= locals.user.color %> 60% 0.2 h);
			--user-col-contrast: white;
			--col-primary: var(--user-col);
		"
	<% } %>
>